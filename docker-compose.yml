services:
  product-api:
    image: product-api:local
    build:
      context: .
      dockerfile: src/ProductApi/Dockerfile
    ports:
      - "8081:8080"

  order-api:
    image: order-api:local
    build:
      context: .
      dockerfile: src/OrderApi/Dockerfile
    ports:
      - "8082:8080"
